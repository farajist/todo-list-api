# https://www.playframework.com/documentation/latest/Configuration

slick.dbs.default {
    profile = "slick.jdbc.PostgresProfile$"
    db {
        driver = "org.postgresql.Driver"
        url = "jdbc:postgres://localhost:5432/todo-list"
        user=local
        password=
        numThreads=20
        maxConnections=20
    }
}
